kind: ImagePolicy
metadata:
  name: insecure-reveal-env-variables-image-policy
spec:
  filterTags:
    pattern: '^main-[a-fA-F0-9]+-(?P<ts>.*)'
    extract: '$ts'
  policy:
     numerical:
      order: asc